# our-backstage

Internal Backstage dev instance (local) used to test plugins like the gamification plugin.

Start with the [Gamification TechDocs](gamification/index.md) for plugin architecture, authorization, data model, API contracts, lifecycle invariants, workflow docs, and maintenance references.
