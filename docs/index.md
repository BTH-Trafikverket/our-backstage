# our-backstage

Internal Backstage dev instance (local) used to test plugins like the gamification plugin.

Start with the [Gamification TechDocs](gamification/index.md). The docs are organized for new project contributors: product behavior first, then system design, API boundaries, operations, and delivery history.
